{
  "name": "matriky",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "version": "node -e \"require('fs').writeFileSync('version.js', \\\"const APP_VERSION='\\\" + process.env.npm_new_version + \\\"';\\\\n\\\")\" && git add version.js"
  }
}
